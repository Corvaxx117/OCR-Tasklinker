Extensions installées via Composer actuellement:

symfony/framework-bundle (skeleton)
symfony/console
symfony/dotenv
symfony/runtime
symfony/yaml
symfony/orm-pack (Doctrine ORM + migrations + bundle)
symfony/twig-pack (Twig)
symfony/form
symfony/validator
symfony/asset
symfony/maker-bundle (dev)
symfony/monolog-bundle
